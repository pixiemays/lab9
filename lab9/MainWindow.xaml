<Window x:Class="lab9.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:lab9"
        mc:Ignorable="d"
        Title="MainWindow" Height="650" Width="725">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ListView Name="ProductsList" ScrollViewer.VerticalScrollBarVisibility="Visible" Height="550" Margin="10">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Black" BorderThickness="1" Margin="5" Padding="5">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="120"/>
                                <ColumnDefinition Width="100" />
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding ProductPhoto}" Width="140" Height="140" Grid.Column="0"/>

                            <StackPanel Grid.Column="1" Margin="10,0">
                                <TextBlock Text="{Binding ProductName}" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ProductDescription}" />
                                <TextBlock Text="Производитель:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ProductManufacturer}" />
                                <TextBlock Text="Цена:" FontWeight="Bold"/>
                                <TextBlock Text="{Binding ProductCost}" />
                            </StackPanel>
                            <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Grid.Column="2">
                                <TextBlock Text="{Binding ProductMeasureText}" 
                                   VerticalAlignment="Center" 
                                   HorizontalAlignment="Center" 
                                   TextWrapping="Wrap"/>
                            </Border>
                            <Button Grid.Column="3" Click="AddProductButton">Редактировать</Button>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Margin="5" Width="100" Height="25" Click="AddProductButton">Добавить товар</Button>
            <Button Margin="5" Width="100" Height="25" Click="ReloadProducts">Релоад стр</Button>
        </StackPanel>
    </Grid>
</Window>
